# SampleFlow 零上下文交接

> 更新时间：2026-09-03（America/Los_Angeles）
>
> 工程基线：PR [#138](https://github.com/Eclipseic1848/SampleFlow/pull/138) 已合并，`origin/main` 为 `6c346c495bce854874a4e7171b01f0b8442c2b4a`；主分支 [quality gate 33726596730](https://github.com/Eclipseic1848/SampleFlow/actions/runs/33726596730) 全部通过。
>
> 当前停点：本地位于同一目录的 `codex/update-handoff-after-138` 分支，HEAD `5f5a61d825e49f42493161e5c487ad0957b44892`。工作树现有 41 个已修改文件和 1 个新增迁移文件，混合包含用户此前未提交工作、全工程审查修复与前端 UX 审查修复；必须整体保护。原“相对 `main` 只有 `handoff.md`”判断已经失效。文档 PR [#139](https://github.com/Eclipseic1848/SampleFlow/pull/139) 保持开放，其 CI [33727571426](https://github.com/Eclipseic1848/SampleFlow/actions/runs/33727571426) 已通过，但没有在本次修复中操作。
>
> 最高优先级：前端 UX 审查的 13 项问题已创建为 GitHub Issue #140—#152，并在本地逐项完成最小修复；最终 Web E2E 51/51、类型检查和生产构建通过。Issues 仍保持 OPEN，当前没有提交、推送、PR、Issue 评论/关闭、部署或真实数据操作；下一步先由用户检查结果，再决定是否授权后续 GitHub 写入。

## 0C. 2026-09-03 前端 UX 审查 Issue 与本地修复完成点

### GitHub Issues

- 已按用户明确授权创建并只读复核 13 个 OPEN Issues：[#140](https://github.com/Eclipseic1848/SampleFlow/issues/140)—[#152](https://github.com/Eclipseic1848/SampleFlow/issues/152)。
- P0：#140 表单字段级校验与首错聚焦；#141 Excel 导入忙碌期禁止关闭；#142 长表单未保存离开保护；#143 审计变更摘要与按需详情。
- P1：#144 审批中心待办分层；#145 后台刷新保留结果；#146 总览穿透失败原地重试；#147 新手引导 Esc 会话语义；#148 组织页中等桌面布局；#149 临时密码安全复制。
- P2：#150 侧栏真实链接；#151 移除全表 MutationObserver 扫描；#152 生产登录状态用户文案。
- 未创建标签、评论或关闭 Issue；这些远端写入不在本次授权内。

### 本地实现

- `noValidate` 业务表单在提交前调用原生 `reportValidity()`，必填字段由浏览器聚焦并提示；没有改变服务端校验。
- Excel 导入请求期间禁用取消、关闭、Esc 与遮罩关闭；账号、组织、订单和目标长表单离开前保护未保存内容。
- 审计主表只显示可扫描的字段变化摘要，完整前后值改为弹窗；未知字段保留真实键名，不伪装成通用文案。
- 审批中心按目标、修改申请、联动选择分层显示数量，默认首个非空分类，并用 `approvalTab` 支持刷新和浏览器历史恢复。
- 总览和目标非首次刷新保留已有结果；总览个人、小组、组织穿透均可在弹窗内重试，同一上下文重复请求会被拦截，不同小组的并发与过期响应隔离保持有效。
- 新手引导 Esc 仅写会话级“稍后再看”，不再反复自动弹出；手动重播会清除该会话标记。
- 组织双表在 1280px 及以下改为单列；创建和重置密码结果共用安全复制交互，失败时仍可手动选择。
- 侧栏改为真实 `href` 链接并保留 SPA 普通点击、权限过滤、历史和当前页状态；所有横向表格滚动区改为渲染时直接提供键盘与 ARIA 语义，已删除主内容全量 MutationObserver。
- 生产登录页只显示用户可理解的系统状态；开发环境仍保留技术诊断文案。

### 验证与保护边界

- `npm.cmd run typecheck --workspace @sampleflow/web`：通过。
- `npm.cmd run build --workspace @sampleflow/web`：通过；生产 bundle 包含用户文案，不包含“前端、API 与数据库已连接”。
- `npm.cmd run test:e2e --workspace @sampleflow/web`：最终完整运行 51/51 通过；覆盖 1280、1024、200% 缩放、WCAG 2.2 A/AA、审批全流程、并发穿透、引导、账号复制、导航、筛选和业务权限。
- 静态检查确认所有 `orders-table-wrap` 在渲染处直接带 `tabIndex={0}`、`role="region"` 与可访问名称；`git diff --check` 通过，仅有 Windows LF/CRLF 提示。
- 当前 41 个已修改文件和 1 个新增迁移文件仍全部未提交且来源混合。禁止 reset、clean、checkout、stash、广泛暂存，禁止为提交或关闭 Issue 改写已经正确的功能。
- 没有运行真实数据、生产服务或业务 UAT；没有提交、推送、创建 PR、评论/关闭 Issue、部署或发布。

## 0B. 2026-09-03 全工程审查修复完成点

### 已完成修复

- 新增迁移 `028_allow_receivable_historical_reviews.sql`：历史核对支持负数 `receivable_pending`，并在数据库层约束状态与金额符号一致。
- 历史核对批准会按事件业务日解析主业务员和协作人的组织快照，保留协作比例及双方归属；负数核对结果继续计入应收未收账本，不再丢失个人、组和部门分摊。
- 记账期间、更正申请、历史核对三类列表改为真实服务端分页，统一默认 10 条/页并保留 10/20/50/100 选择；旧的固定 18/30 条截断已移除。
- 业绩分析客户筛选写入 `analysisCustomerFilter`，可通过刷新和浏览器前进/后退恢复。
- README 与 P1 规格中的分页默认值统一为 10 条；没有改动已经正确的分页选择能力。
- 审计查询的过紧 1 秒测试看门狗放宽为 10 秒；桌面验收改为等待可观察的新手引导完成状态，移除标题焦点与引导处理竞态。

### 回归证据

- `npm.cmd run verify`：退出码 0。
- 类型检查：API 与 Web 全部通过。
- API：168/168 通过；包含新增的治理真分页、负数历史核对、协作归属与 001-028 干净迁移/升级测试。
- Web E2E：51/51 通过；覆盖 1280、1024、200% 缩放、分析 URL 恢复、弹窗穿透、订单治理及新手引导。
- 容器合同：11/11 通过；生产构建通过。
- 容器运行验收：通过隔离首装、027→028 真实升级、ready/smoke、备份、新库恢复、最小权限和测试资源清理。
- `npm audit --omit=dev`：0 个漏洞；Compose 配置校验通过；`git diff --check` 通过，仅有 Windows 换行提示。
- 验证只使用临时隔离 PostgreSQL；没有读取真实 Excel、连接生产服务或修改真实业务数据库。业务 UAT 仍由有权用户完成。

### 当前保护边界与下一步

- 当前 30 个已修改文件和新增迁移均未提交；其中包含本次修复前已经存在的用户改动，禁止 reset、clean、checkout、stash、广泛暂存或为提交而改写正确功能。
- 本次没有 GitHub、PR、Issue、部署或发布操作。
- 下一步只应先检查本节、`git status --short --branch` 和当前 HEAD；用户确认功能后，才按其明确授权处理提交或远端工作。

## 0A. 2026-09-03 全工程代码审查记录（已由 0B 修复结论取代）

### 审查范围与限制

- 已完整理解并检查主要代码路径：React 19/Vite 前端、Fastify 模块化单体 API、PostgreSQL 迁移、权限/数据范围、导入、订单、业绩分析、审计、容器和工程门禁。
- 审查基线包含当前未提交改动，不只检查 HEAD。所有受 Git 跟踪的代码均可访问。
- 为保护敏感业务数据，本次未打开真实 Excel 的内容；也没有访问真实生产服务器、真实账号、生产数据库或代替业务人员完成 UAT。
- 审查期间没有修改功能代码、测试或数据库。本节是用户离开前明确要求留下的审查记录。

### 当前工作树与保护边界

- 分支：`codex/update-handoff-after-138`
- HEAD：`5f5a61d825e49f42493161e5c487ad0957b44892`
- `git status --short` 显示 23 个已修改文件：`DESIGN.md`、`UX-CONTRACT.md`、API 6 个文件、Web E2E 7 个文件、Web 源码 4 个文件、规格文档 2 个文件及 `handoff.md`。
- 这些变更全部按用户现有工作处理。禁止为提交、合并、清理或其他任务私自修改已经正确的功能；禁止 broad stage、reset、clean、checkout 或 stash。

### 审查结论

未发现 Critical。确认 2 个 High、4 个 Medium、1 个 Low。除下列问题外，认证/会话/CSRF/Origin、常规权限、目标审批、普通更正、导入安全、数据库不可变审计和容器最小权限未发现明显的 Critical/High 问题；未发现有证据支持的 N+1 问题。

#### High 1：历史核对无法表达负数“应收未收”结论

- 业务依据：`CONTEXT.md:88-109` 定义历史核对及负数应收语义。
- 代码依据：`apps/api/src/modules/accounting-periods.ts:30-43` 的核对状态只允许 `active/paused/zero`，金额使用非负 schema；`apps/api/migrations/015_accounting_period_governance.sql:68-88` 也只允许上述状态和非负金额；`apps/web/src/pages/orders-page.tsx:205` 没有 `receivable_pending` 选项。
- 对照依据：主订单表已经支持负数和 `receivable_pending`，见 `apps/api/migrations/026_sheet3_order_input.sql:5-16`。
- 影响：合法业务结论无法提交，或用户只能填入错误状态/金额。
- 最小修复：历史核对加入 `receivable_pending`，核对金额改为有符号金额；同步数据库约束、审批后的计入金额逻辑、前端选项和回归测试。

#### High 2：协作订单经历史核对批准后丢失协作分摊

- 业务依据：`docs/adr/0031-use-sheet3-order-input-and-split-collaboration.md:7-9`、`CONTEXT.md:108-109` 要求后续调整保留协作比例，并冻结双方组织归属。
- 代码依据：`apps/api/src/modules/accounting-periods.ts:484-514` 只读取和写入主业务员；`apps/api/migrations/026_sheet3_order_input.sql:65-78` 在协作字段为空时会把 100% 归给主业务员。
- 正确现有模式：`apps/api/src/modules/performance.ts:1793-1820` 已有可复用的双方组织快照与协作字段写入路径。
- 影响：公司总额可能仍能核平，但主业务员、协作人、组别和部门业绩会归属错误。
- 最小修复：审批时读取协作人/比例，按事件日期解析双方组织快照，写入完整协作字段，并增加协作订单历史核对回归测试。

#### Medium 1：核对治理列表是假分页并会静默截断

- 位置：`apps/api/src/modules/accounting-periods.ts:26-48` 默认限制 30；期间列表 `:153-168` 限制 18；更正列表 `:171-194` 限制 30；历史核对列表 `:402-420` 限制 30；`apps/web/src/pages/orders-page.tsx:173-181,207` 只取一次结果后在前端分页。
- 影响：较旧记录甚至待处理记录可能无法到达。
- 修复：服务端统一 `page/pageSize/totalCount`，默认每页 10 条，保留 10/20/50/100 选择。

#### Medium 2：业绩分析客户筛选不能通过 URL 恢复

- 位置：`apps/web/src/pages/analysis-page.tsx:18-19,33-45` 只把筛选放在组件本地状态；`UX-CONTRACT.md:55-63,86` 要求可恢复状态；`apps/web/e2e/analysis.spec.ts:116-126` 只验证即时筛选，没有刷新/前进后退验证。
- 影响：刷新、分享链接和浏览器前进后退会丢失筛选条件。
- 修复：增加 `analysisCustomerFilter` URL 参数及 `popstate` 同步，并补一条刷新/历史导航 E2E。

#### Medium 3：分页默认值文档互相冲突

- 正确实现/口径：`UX-CONTRACT.md:61`、`docs/specs/p1-product-closure.md:40`、`apps/web/src/shared-ui.tsx:5-22` 均为默认每页 10 条，并保留 10/20/50/100 选择。
- 失效文字：`README.md:15`、`docs/v1-scope.md:33`、`docs/specs/p1-product-closure.md:103`、`docs/specs/p1-ticket-breakdown.md:122,276` 仍写 20 条；同一 ticket 文档 `:122` 与 `:127` 自相矛盾。
- 影响：验收和后续实现可能按错误口径回退正确功能。
- 修复：只修正文档为 10 条，不改已经正确的运行时功能。

#### Medium 4：原交接状态已过期

- 位置：本文件原顶部和第 8.0 节声称工作树只有 `handoff.md` 差异。
- 影响：下一会话可能误判 22 个用户改动为异常并清理、覆盖或绕开。
- 修复：本次记录已纠正；以后以本节列出的分支、HEAD 和 23 个修改文件为保护基线，并在恢复时只读复核。

#### Low：两条全量测试存在非确定性竞态

- API：全量运行 165/166，`apps/api/src/audit-query.integration.test.ts:224-230` 的 1 秒 `Promise.race` 超时；单文件复跑 4/4 通过。
- Web E2E：全量运行 50/51，1024/200% 场景中标题聚焦与新手引导竞态；涉及 `apps/web/src/onboarding.tsx:259-273`、`apps/web/e2e/support/full-stack.ts:102-111`、`apps/web/e2e/desktop-acceptance.spec.ts:180-201`；精确单例复跑 1/1 通过。
- 影响：CI 可偶发红灯并掩盖真实回归；当前证据不足以把它们写成稳定产品缺陷。
- 修复：使用可观察状态做确定性同步，去掉固定 1 秒时限和标题/引导抢焦点假设。

### 本次验证证据

- `npm.cmd run typecheck`：通过。
- `npm.cmd audit --omit=dev`、`npm.cmd audit`：均为 0 个漏洞。
- `npm.cmd run test:container-contract`：11/11 通过。
- `docker compose --env-file .env.example config --quiet`：通过。
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示。
- API 全量：165/166；上述超时单文件复跑 4/4。
- Web E2E 全量：50/51；上述 1024/200% 单例复跑 1/1。
- `npm.cmd run build`：通过。主入口约 405.14 KB，gzip 约 114.81 KB；地图约 22.19 KB，gzip 约 8.70 KB，当前没有必须立即拆包的证据。
- `npm.cmd run test:container-runtime`：通过，覆盖隔离镜像构建、数据库角色、迁移 001-027、API/Web 健康、smoke、升级、备份校验、恢复到新库、恢复后登录和测试资源清理。
- 没有声称 `npm verify` 全量绿色：两次全量测试各有一条上述非确定性失败，必须如实保留。

### 用户返回后的精确恢复顺序

1. 先只读检查本节、`git status --short --branch`、当前 HEAD；若除预期 23 个文件外还有变化，停止并报告。
2. 未经用户明确要求，不执行实现、数据库操作、Git 提交/推送/合并或远端写入。
3. 用户要求修复审查问题时，先处理两个 High：负数应收历史核对、协作订单历史核对归属；每项留下最小回归测试。
4. 再处理治理列表服务端分页、分析筛选 URL 恢复、分页文档口径和两条竞态测试。
5. 只改与问题直接相关的行；不得为了提交 GitHub 或任何其他任务修改已经正确的功能。
6. 完成后重跑受影响测试、类型检查、全量 API/E2E、构建和容器门禁。真实账号、生产环境与业务 UAT仍需另行授权和人工验收。

## 0. 接手后先做什么

1. 只在现有目录 `F:\new branch\SampleFlow` 工作，不创建额外 worktree 或本地副本。
2. 先只读复核：`git status --short --branch`、`git rev-parse HEAD`、`git rev-parse origin/main`、`gh issue list --state open --label P1`、`gh run list --branch main --limit 5`。
3. 阅读本文件、`CONTEXT.md`、`docs/adr/` 和目标 Issue；不要从旧聊天推断易失状态。
4. 如果用户只说“继续”，先执行第 8.0 节的现场恢复。页面恢复且用户确认后，才可继续其他 P1 工作。
5. 未经用户明确同意，不合并 PR #139，不切换分支，不重启服务，不修改代码或数据库。

## 1. 项目、目标与权限边界

SampleFlow 是面向桌面 Web 的销售订单、业绩、目标、组织、账号和审计管理系统。主要技术栈：React 19、Vite 8、Fastify 5、TypeScript、PostgreSQL 16、Docker Compose、npm workspaces。

本阶段目标是完成当前版本 GitHub 上所有 P1 任务。用户已授权代理基于证据自行执行常规工程判断：实现、测试、提交、推送、PR、CI 通过后的合并与 Issue 关闭。以下事项仍保留人工门禁：

- GitHub Release、npm/PyPI 等真实外部发布；
- 密钥、权限、生产数据和不可逆操作；
- 业务角色逐项 UAT 的真实结论；
- 公司服务器、域名、HTTPS、正式备份与监控的现场验收。

其他已确认边界：

- 当前没有移动端，Issue #57 已取消；只维护桌面 Web。
- 当前没有公司服务器，不得把本机或 CI 的 Compose 通过冒充公司服务器验收。
- 不需要手写签名。业务 UAT 仍需由有权人员留下可追溯结论，GitHub 评论或系统审计记录即可。
- 用户提供的 `原始数据1.xlsx` 已通过其业务审核，可以按真实含义实现并在本地验收库呈现；它仍属于敏感业务数据，不得提交 Git、上传 GitHub、写入公开日志或放入测试夹具。

## 2. 已完成的功能

### 2.1 Sheet3“分子”统一业务口径

Excel 导入、模板和人工新建订单已统一到工作表“分子”的字段模型：

- 收样月份
- 日期
- 订单编号（来源于轻流系统）
- 客户姓名
- 客户单位
- 省份
- 业务员
- 部门
- 组别
- 系统营业额
- 服务类型
- 备注
- 兼容空白列
- 协作人
- 协作比例

导入继续执行工作簿安全检查、精确表头校验、整批预检、组织任职有效期校验、原子确认、幂等与重复检测、行级错误汇总。下载模板是合成空模板，不包含真实数据。

### 2.2 金额和订单生命周期

- 系统营业额允许负数；负数表示“应收未收”，不是非法输入。
- `receivable_pending` 生命周期及后续调整已经打通。
- 金额比较使用按分舍入后的等值，避免大额两位小数被浮点误差误判。
- 协作人和协作比例会按有效组织快照归属个人、组别和部门业绩。

### 2.3 订单列表和数据呈现

- 订单业绩表已展示 Sheet3 主要字段，而不是缩减字段集。
- 左侧冻结：收样月份、日期、订单编号。
- 右侧冻结：计入业绩、状态、操作。
- 中间字段可横向滚动；列宽和间距已压缩到桌面可读范围。
- 所有主要数据表统一支持 10、20、50、100 条/页，并可点击页码。
- 省份来自已审核工作簿及组织映射；有值时显示真实值，没有证据时显示“待补齐”，不伪造。

### 2.4 业绩分析

- 分析范围支持按月和按年。
- 年度模式提供 12 个月趋势、客户明细穿透和权限范围内汇总。
- 趋势图支持鼠标悬停/键盘聚焦显示月份与金额，并支持年份切换。
- 省份地图由真实账本数据驱动；台湾省轮廓保留，南海插图不展示。当前工作簿没有台湾业务记录，因此台湾没有业绩数值是正确结果。
- 分析结果严格按当前角色和权限范围返回，不以浏览器筛选代替后端授权。

### 2.5 其他前端体验

- 页面品牌占位字母已替换为真实公司 Logo。
- 搜索框和分页控件的重叠、错位已修复。
- 地图下方不必要的资料缺失/来源说明已移除。
- 基于角色和权限的新手引导已实现：首次自动播放、完成后不再自动打扰、可随时重播、支持跳过、键盘操作、遮罩、高亮、ARIA 与焦点管理。

### 2.6 迁移与历史数据修复

迁移 `027_repair_legacy_import_dates.sql` 修复了旧导入器造成的日期整体偏移一天，以及只用于缺失备注的固定占位文本。它不是通用猜测修复，只允许对以下已审核来源执行：

- 工作簿 SHA-256：`926AAD3D8C59CC356094EB1ABC0CA1FCB3392EAE5867F2B7C0E2BB50BB5C01CF`
- 4,701 条事件
- 2,850 个规范订单
- 总额 `14,675,659.07`
- 原错误日期和 2,711 条占位备注必须精确匹配
- 8 个月的行数、金额必须逐月精确匹配
- 每条记录必须有对应的不可变审核导入凭据和维度回填凭据，日期及回填原因逐行一致
- 不允许存在已关闭期间或后续非遗留事件

任何总额相同但月份分布、行级凭据或来源 SHA 不同的数据库都会被迁移拒绝。迁移在事务内保存前后证据，再修复事件日期、会计月份、订单收样日期和空备注。金额、人员、组织、订单、服务类型和来源证据保持不变。

当前本地数据库的 `027` 迁移文件 SHA-256 为 `b9772a8ceac13b030d5c2e81e07ee2d226aede8c2625d98480dde347ee16ea2a`。

## 3. 真实工作簿和本地数据状态

工作簿：`F:\new branch\SampleFlow\原始数据1.xlsx`

- 文件大小：492,377 字节
- SHA-256：`926AAD3D8C59CC356094EB1ABC0CA1FCB3392EAE5867F2B7C0E2BB50BB5C01CF`
- 权威工作表：“分子”
- 有效事件：4,701 条
- 规范订单：2,850 个
- 总额：14,675,659.07
- 日期范围：2026-01-04 至 2026-08-26
- 当前导入待处理行：0

| 月份 | 事件数 | 金额 |
| --- | ---: | ---: |
| 2026-01 | 635 | 2,314,819.55 |
| 2026-02 | 455 | 1,252,546.10 |
| 2026-03 | 560 | 1,346,159.95 |
| 2026-04 | 520 | 1,989,517.64 |
| 2026-05 | 474 | 1,499,121.10 |
| 2026-06 | 631 | 2,234,990.59 |
| 2026-07 | 778 | 2,487,624.14 |
| 2026-08 | 648 | 1,550,880.00 |

本地验收库最终核对：

- 事件/订单/总额/日期范围与上表一致；
- 4,701 条事件修复证据，2,850 条订单修复证据；
- 错误会计月份 0 条；
- 年度 API：映射账本与账本均为 4,701 条、14,675,659.07，待处理 0，核对一致；
- 工作簿没有台湾省业务行，地图保留台湾轮廓但不应伪造数值。

组织维度说明：本地仅为完成真实数据呈现配置了经用户批准的最小组织映射。部门、组别、负责人、主管和生效日期的公司权威清单仍需业务方提供，现有本地映射不能直接当作生产组织主数据。

## 4. 已有工程证据及其边界

PR [#138](https://github.com/Eclipseic1848/SampleFlow/pull/138) 已于 2026-09-03 合并。相关 Issue #135、#136、#137 已关闭。

PR #138 合并前后的自动化与本地验证记录如下。这些是保留的历史证据，不代表最后一次人工查看时页面仍正常：

- API 全量测试：166/166 通过。
- Web 端到端测试：51/51 通过。
- API、Web 生产构建通过；Vite 完成 1,940 个模块构建。
- API 和 Web 类型检查通过。
- 容器契约：11/11 通过。
- 隔离 Compose：首次安装、真实升级、ready/smoke、备份、恢复到新库、最小权限和恢复后登录均通过，测试资源已清理。
- `npm audit --omit=dev`：0 个漏洞。
- `docker compose config --quiet` 和 `git diff --check` 通过。
- 两轮独立审查完成；最终未发现阻断项。

主分支 [quality gate 33726596730](https://github.com/Eclipseic1848/SampleFlow/actions/runs/33726596730) 成功，包含类型、测试、契约、构建、隔离 Compose 首装/升级/恢复、依赖审计和配置门禁。

自动化通过能证明代码与隔离运行路径，不能证明当前开发服务没有失效，不能替代用户正在进行的页面验收，也不能证明业务 UAT 或公司服务器验收已经完成。

文档 PR #139 只包含 `handoff.md`，CI 已通过但未合并。不要用 #139 的通过结论覆盖最后一次页面异常。

## 5. 当前未解决的本地页面异常

最后一次用户人工查看的事实：

- 页面地址使用 `http://localhost:5174`。
- 省份地图区域报错：`Failed to fetch dynamically imported module: http://localhost:5174/src/china-map.ts`。
- 页面当时选择 2026 年 9 月，省份汇总和客户单位汇总为空。
- 工作簿真实数据只覆盖 2026 年 1 月至 8 月；9 月空数据本身可能符合数据范围，但“为何进入 9 月”和“地图模块为何加载失败”均未诊断，不能先下结论。
- 在此之前用户刚完成过正常页面测试。
- 页面异常出现前，代理从 `main` 切到只改 `handoff.md` 的文档分支；该时间关系不是根因证明。
- 异常出现后没有修改业务代码、没有重启服务、没有恢复数据库；用户要求立即暂停。

历史端口配置仍为 Web `5174`、API `3000`、PostgreSQL 宿主机 `55432`，但交接时没有重新探测其运行状态。不要写成“当前服务正常”。

## 6. 本地备份与恢复边界

这些备份位于 Git 外，不得上传或提交：

| 用途 | 路径 | SHA-256 |
| --- | --- | --- |
| 省份回填前 | `C:\Users\55672\AppData\Local\Temp\sampleflow-before-province-backfill-20260902.dump` | `16177426F4C17C2F6005ECD0D55F6D8BA4A2DA6D36979047AFFAA4A8DCCA22BB` |
| 维度回填后、日期修复前；首选重放基线 | `C:\Users\55672\AppData\Local\Temp\sampleflow-after-dimension-backfill-before-date-repair-20260902.dump` | `A7F45A3BADAB31CF66096AF3856379C4C7844E972DA3CCE237A5B8BB26D71FB4` |
| 初版 027 后、最终 027 重放前 | `C:\Users\55672\AppData\Local\Temp\sampleflow-current-before-final-027-20260902.dump` | `7370A72587E9157D64E29B056BF98B996274B16D6B8272702B03B44E47C9871C` |

恢复时优先使用第二个备份，然后运行当前代码的正式迁移命令。第三个备份含旧版 `027` 迁移哈希，不能作为最终代码的稳定恢复基线。任何恢复操作都必须先确认目标数据库、备份校验和与恢复范围，禁止覆盖未知数据库。

## 7. GitHub 当前 P1 状态

接手时必须实时复核，以下是 2026-09-03 的已验证快照：

| Issue | 状态 | 为什么仍开放 |
| --- | --- | --- |
| [#12](https://github.com/Eclipseic1848/SampleFlow/issues/12) 恢复省份与客户单位多维分析及明细穿透 | `ready-for-human` | 工程已完成，等待 #63 业务 UAT |
| [#15](https://github.com/Eclipseic1848/SampleFlow/issues/15) 生产安全、备份恢复、可观测性与部署验收 | `ready-for-human` | 工程与隔离 Compose 已通过，等待 #64 公司服务器验收 |
| [#63](https://github.com/Eclipseic1848/SampleFlow/issues/63) TB-27 真实数据能力验收与业务 UAT | `ready-for-human` | 仍缺公司权威组织映射和各角色桌面业务 UAT 结论 |
| [#64](https://github.com/Eclipseic1848/SampleFlow/issues/64) TB-28 公司服务器实际验收 | `ready-for-human` | 当前没有公司服务器、域名、HTTPS、正式密钥、备份存储、监控与运维验收环境 |
| [#65](https://github.com/Eclipseic1848/SampleFlow/issues/65) TB-29 P1 完成审计与 Issue 收口 | `ready-for-human` | 必须等待 #63、#64 及父 Issue 真正完成 |

最新证据已分别更新到：

- [#63 真实数据证据评论](https://github.com/Eclipseic1848/SampleFlow/issues/63#issuecomment-5521967156)
- [#64 工程与服务器边界评论](https://github.com/Eclipseic1848/SampleFlow/issues/64#issuecomment-5521967925)
- [#65 收口审计评论](https://github.com/Eclipseic1848/SampleFlow/issues/65#issuecomment-5521969169)

没有可诚实关闭的 `ready-for-agent` P1 Issue。不要因为“工程已完成”就关闭以上人工门禁。

## 8. 精确下一步

### 8.0 先验收本次未提交修复

1. 先读第 0B 节，再只读确认 `git status --short --branch` 和当前 HEAD；保护当前 30 个已修改文件及新增迁移，不能把用户此前工作与本次修复拆错或清理。
2. 如需复核工程证据，在隔离测试数据库运行 `npm.cmd run verify`；不得对真实 Excel、真实账号或生产数据库执行测试。
3. 由用户检查历史核对的负数应收、协作归属、三类治理分页和分析筛选恢复；工程门禁通过不替代业务 UAT。
4. 用户确认前不提交、推送、合并 PR #139 或推进 #63/#64/#65；若用户授权 Git 操作，只精确暂存经确认的文件，不得顺手修改正确功能。

### 8.1 有公司权威组织资料时，推进 #63

1. 取得部门、组别、人员、负责人/主管、稳定人员标识和生效日期的权威清单。
2. 在明确目标库和备份后导入或配置，不从姓名或历史工作簿猜测组织关系。
3. 由有权限的业务人员按角色验证：登录、订单查询/横向滚动/冻结列、导入预检与确认、人工新建、负数应收未收、协作归属、月/年分析、趋势提示、省份/客户穿透、导出、审计。
4. 在 #63 留下角色、环境、数据版本、通过/失败项和问题链接。无需手写签名，但必须由有权人员留下可追溯结论。
5. #63 真正通过后再关闭 #12。

### 8.2 公司服务器准备好时，推进 #64

1. 确认服务器、操作系统、域名、网络边界、TLS、正式密钥注入方式、备份目标、监控告警、RPO/RTO 和维护窗口。
2. 先备份并记录恢复点，再按部署文档执行；不得把开发密钥或本地数据直接复制到服务器。
3. 验证 HTTPS、权限、最小数据库权限、迁移、ready/smoke、备份、恢复到新库、日志、指标、告警和回滚。
4. 由运维/业务负责人记录现场结论后关闭 #64，再关闭 #15。

### 8.3 最终关闭 #65

只有 #12、#15、#63、#64 均以真实证据关闭，且主分支 CI 仍为绿色时，才更新 P1 审计表并关闭 #65。若需要 GitHub Release 或外部发布，必须另获用户明确授权。

## 9. 总体 Roadmap 与版本计划

### 当前 P1：真实业务可用版

- 工程代码、真实数据映射、迁移安全、自动化和隔离部署门禁已合并并通过；最后一次本地页面人工查看出现未诊断异常，现场恢复完成前不能宣告桌面 Web 验收完成。
- 本地页面恢复：最高优先级，等待用户允许继续。
- 业务 UAT：等待 #63。
- 公司服务器验收：等待 #64。
- P1 总审计：等待 #65。

### P1 完成后的候选版本

1. 发布候选版：只在 #63/#64 通过后冻结候选 SHA，补齐发布说明和回滚点。
2. 首个正式版：需要用户另行授权真实外部发布；以公司服务器现场验收、备份恢复和监控为前置条件。
3. P2：只从新 Issue 和真实使用反馈进入，不提前添加移动端、复杂缓存、第二套导入模型或未经确认的分析维度。

## 10. 绝对不要再踩的坑

1. 用户正在本地验收时，不要切换分支、重启服务或做 GitHub 收口；先保持现场稳定并等待其完成。
2. 页面刚验证正常后又异常，先恢复同一运行态并证明根因；不要直接开启第二轮代码修复。
3. 不要用 CI 绿色替代当前页面可用性，也不要把时间相关性写成已确认根因。
4. 不要创建额外 worktree 或新的本地项目副本；用户明确不需要。
5. 不要把本机、CI 或隔离 Compose 通过写成“公司服务器验收通过”。
6. 不要为关闭 Issue 伪造业务 UAT、组织主数据、台湾业绩或生产证据。
7. 不要把“取消手写签名”误解为取消可追溯的人工验收结论。
8. 不要把真实工作簿、真实行、截图、数据库 dump、密钥或组织映射提交到 Git/GitHub。
9. 不要再次按旧导入器逻辑把 Excel 日期减一天。
10. 不要仅凭总行数和总金额修复历史数据；必须验证来源 SHA、逐月分布和行级不可变凭据。
11. 不要让兼容逻辑对任意来源 SHA 生效；只有已审核来源能进入受控历史修复。
12. 不要禁止负数系统营业额；它表示应收未收。
13. 不要用姓名猜测不唯一人员、负责人或组织归属；使用稳定标识和生效日期。
14. 不要把省份空值替换成编造值；无证据时显示“待补齐”。
15. 不要因为台湾没有业务数值就删除台湾轮廓；也不要恢复南海插图。
16. 不要把前端过滤当授权；分析、明细和导出必须由 API 按角色裁剪。
17. 不要破坏表格冻结列顺序或让固定列覆盖中间滚动内容。
18. 不要恢复固定 `1e-7` 金额比较；金额按分舍入比较。
19. 不要用测试通过代替真实角色的产品验收。
20. 不要 broad stage、reset、clean、checkout 或 stash 用户文件；提交只使用精确文件白名单。
21. 不要直接恢复第三个旧 `027` 哈希备份；优先从“维度回填后、日期修复前”备份重放当前迁移。
22. 不要自动创建 Tag、Release 或部署到外部环境。

## 11. 接手时的只读复核命令

```powershell
Set-Location -LiteralPath 'F:\new branch\SampleFlow'
git status --short --branch
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
gh issue list --repo Eclipseic1848/SampleFlow --state open --label priority:P1 --limit 100
gh pr list --repo Eclipseic1848/SampleFlow --state open --limit 50
gh run list --repo Eclipseic1848/SampleFlow --branch main --limit 5
docker compose ps
Invoke-RestMethod 'http://127.0.0.1:3000/api/ready'
```

如果 Docker 或开发服务未启动，最后两条失败只说明本地运行态不存在；不要据此否定已经留存的 CI 证据，也不要在未确认目标库时执行恢复或迁移。

## 12. 权威文档入口

- `README.md`：使用、导入、运行和验证入口。
- `CONTEXT.md`：领域边界和业务规则。
- `docs/adr/`：关键架构决策，尤其是 Sheet3、负数、协作归属和历史数据修复。
- `docs/agents/issue-tracker.md`：Issue 操作边界。
- `docs/agents/triage-labels.md`：标准标签定义。
- GitHub Issues/PR/Actions：远端易失状态的最终依据。

本文件只记录截至更新时间的已验证事实。分支、SHA、CI、Issue、运行中的容器和服务都可能变化，必须实时复核。
