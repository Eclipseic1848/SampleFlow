# SampleFlow 零上下文交接

> 更新时间：2026-09-01（America/Los_Angeles）
>
> 当前结论：**当前版本中可由工程代理完成的 P1 桌面 Web 与仓库生产准备已经完成实现与本地验收，交付载体是 PR #102；PR、CI、`main` 和 Issue 的最终远端状态必须实时查询。P1 仍有真实数据业务 UAT 与公司服务器验收两个人工 Gate；当前没有真实数据授权，也没有服务器，因此不得伪造完成。**

## 1. 当前任务

SampleFlow 是销售业绩与目标管理 Web 系统，技术栈为 React 19、Vite 8、Fastify 5、TypeScript、PostgreSQL 16 和 Docker Compose，仓库采用 npm workspaces 模块化单体。

本轮任务是：

1. 全面审查当前工程；
2. 为真实发现的问题创建 GitHub Issues；
3. 修复当前版本中所有可由工程代理完成的 P1 问题；
4. 完成测试、提交、推送、PR、CI 绿后合并与 Issue 关闭；
5. 保留真实数据、服务器、发布、密钥和不可逆操作的人工门禁。

用户已明确：

- 当前只做桌面 Web，没有移动端；#57 已按不计划取消。
- 当前没有公司服务器，不执行或假装执行真实服务器验收。
- 地图必须包含台湾省；前端不需要南海展示。
- 前端使用真实公司 Logo，不使用 “SF” 占位标。
- 目标确认只取消手写签名；保留登录身份、目标版本、固定确认声明、时间和不可修改审计。
- 常规实现、测试、提交、推送、PR、CI 绿后合并和 Issue 关闭可依据证据自行执行。
- GitHub Release、Tag、真实外部发布、密钥与权限、真实/生产数据和不可逆操作仍需人工批准。

## 2. 权威状态

### 2.1 Git 与 GitHub

- 仓库：https://github.com/Eclipseic1848/SampleFlow
- 默认分支：`main`
- 本轮交付 PR：#102 `fix: close P1 audit hardening gaps`
- PR #102 配置为合并时自动关闭 #94、#95、#96、#97、#98、#99、#100；实际状态以 GitHub 实时结果为准。
- 本地路径：`F:\new branch\SampleFlow`
- 本轮工作分支：`codex/audit-hardening-p1`；接手时以 `git branch --show-current` 为准。
- 本轮没有新建 worktree 或额外项目文件夹。
- 当前没有 Git Tag 或 GitHub Release。
- `package.json` 的 `1.0.0` 且 `private: true` 不是外部发布证据。

新会话必须先实时执行以下只读检查，不要依赖本文中的旧 SHA：

```powershell
git status --short --branch
git fetch origin main
git rev-parse HEAD
git rev-parse origin/main
gh pr view 102 --repo Eclipseic1848/SampleFlow
gh issue list --repo Eclipseic1848/SampleFlow --label "priority:P1" --state open
gh run list --repo Eclipseic1848/SampleFlow --limit 10
```

### 2.2 当前 P1 状态

工程代理已完成：

- #10、#11、#13、#14 及 #37—#62 已关闭；#57 是取消的移动端范围。
- 审查修复 #94—#100 已进入 PR #102，并配置为合并时自动关闭。
- P1 的桌面 Web、后台、分析、审计、安全、可观测性、备份恢复和仓库部署门禁均有自动化证据。

仍开放且必须保持 `ready-for-human`：

| Issue | 现状 | 关闭条件 |
| --- | --- | --- |
| #63 真实数据与业务 UAT | 没有真实数据访问授权和业务验收条件 | 在批准的隔离环境完成来源冻结、对账、幂等重放、角色 UAT 与业务签字 |
| #64 公司服务器实际验收 | 当前没有服务器 | HTTPS、访问范围、正式秘密、备份、监控、RPO/RTO、升级回退与运维签字齐全 |
| #12 多维分析父项 | 工程能力已完成，等待 #63 | #63 通过后关闭 |
| #15 生产准备父项 | 仓库能力已完成，等待 #64 | #64 通过后关闭 |
| #65 P1 总验收 | 依赖 #63、#64、#12、#15 | 所有人工 Gate 真实通过后关闭 |

P2 #101“修正账号分页深链的上一页语义”已记录但未开始；它不是当前 P1 阻断项。不要在没有新指令时自动进入 P2。

## 3. 本轮完成内容

### 3.1 数据库迁移完整性（#94）

- 迁移账本新增 SHA-256，启动就绪检查同时校验缺失、未知和内容被修改的迁移。
- 旧账本可审计补齐哈希；并发迁移继续使用数据库锁并保持幂等。
- 哈希计算先统一 CRLF/LF，避免 Windows 与 Linux checkout 产生假差异。
- `.gitattributes` 固定 SQL 为 LF、批处理为 CRLF。
- 当前迁移范围为 `001`—`024`；`024_auth_throttle_cleanup.sql` 增加认证限流清理索引。

### 3.2 认证、授权与 bigint（#95）

- 不存在或停用账号仍执行 dummy scrypt，减少账号枚举时间差。
- 改密使用密码版本 CAS；相同旧密码的并发请求最多一个成功。
- 管理员角色与状态修改串行保护“至少一个启用的系统管理员”不变量。
- 用户、人员、目标、订单、更正、导入、正式报表等 PostgreSQL bigint ID 全链路使用十进制字符串，避免 JavaScript 安全整数精度别名。
- Excel 可选更正授权 ID：空单元格被省略，文本 bigint 保真，安全整数转字符串，非法数值交由预检阻断。

### 3.3 Web 会话、写入恢复与可复现 URL（#96、#97）

- 所有受保护请求统一走 `apiFetch`；只有 401 触发会话过期，403 保留在当前页面显示无权限。
- 四类 CSV/文件导出改为受保护 fetch + blob，不再用页面跳转绕过会话处理。
- 退出失败或网络结果不确定时保留本地会话，禁止重复提交并允许安全重试。
- 改密、账号、组织、目标等写入补齐 pending、明确失败恢复与不确定结果刷新。
- 总览区分加载、失败、空数据和成功。
- 分析、审计、账号页的筛选和分页写入 URL；刷新和浏览器前进/后退可以复现当前结果。
- 分析客户深链可自动追到后续游标页；审计分页使用稳定快照游标。

### 3.4 生产边界与维护（#98）

- Nginx 上传上限与 API 30 MiB 合同一致，容器验收包含超过默认 1 MiB 的真实代理请求。
- 增加会话和登录限流记录的保留期清理命令、Compose `operations` 作业和部署文档调度示例。
- 部署文档修正备份失败即停止、保留策略、恢复与升级回退流程。

### 3.5 权威文档（#99）

- `CONTEXT.md`、`README.md`、`UX-CONTRACT.md`、CI 名称、`start_all.bat` 和部署文档与当前桌面 Web 范围同步。
- 新增 ADR 0028—0030，固定事件分析维度、审计可见范围和已认证目标确认决策。
- `docs/specs/p1-product-closure.md` 与 `p1-ticket-breakdown.md` 已纳入仓库，不再保留“尚未获得常规 GitHub 写入授权”的失效文字。
- `start_all.bat` 保持 CRLF，并在 Windows 使用 `npm.cmd`。

### 3.6 稳定审计游标（#100）

- 审计游标绑定用户、过滤条件、当前位置和快照上界。
- 首页在事务中等待在途写入并固定上界；后续页不会混入翻页过程中新增的记录。
- 复用其他用户或不同过滤条件的游标会被拒绝。

## 4. 完整验收证据

本轮稳定源码执行 `npm.cmd run verify`，结果：

- API 测试：158/158。
- Web Playwright：39/39；正式验收项目为桌面 1024px 与 1280px，若干既有测试内部仍显式切换到 390×844，但不构成移动端产品承诺。
- 容器契约：11/11。
- API/Web 类型检查：通过。
- 生产构建：通过。
- 隔离 Compose：首装、真实升级、ready/smoke、超过默认代理上限的请求、备份、新库恢复、最小权限和恢复库登录全部通过。
- `npm audit --omit=dev`：0 vulnerabilities。
- `docker compose --env-file .env.example config --quiet`：通过。
- Spec 审查：无 P0/P1。
- Standards 审查：无 P0/P1。
- 最终相邻路径复核：无有代码证据的 P0/P1。
- PR #102 required check、合并与 Issue 关闭结果：必须在 GitHub 实时复核，不由本地证据替代。

这些证据只证明仓库代码、合成数据与隔离环境，不证明真实数据 UAT、公司服务器、生产部署或外部发布。

## 5. 当前卡点

没有已知可继续由工程代理修复的 P1 代码缺陷。剩余卡点都是缺少外部条件：

1. #63 缺真实数据授权、权威组织/区域映射、批准的验收环境和业务验收人。
2. #64 缺公司服务器、域名/HTTPS、正式秘密、备份存储、监控、RPO/RTO 与运维责任人。
3. #65 必须等待上述两项以及父项 #12/#15 真实关闭。
4. 当前没有发布授权，不创建 Tag 或 GitHub Release。

## 6. 下一步计划

### 6.1 真实数据就绪后

1. 冻结数据来源哈希、字段映射、权威组织/区域映射、批准人、目标验收库、备份和恢复路径。
2. 真实数据只进入明确批准的隔离环境；不得把姓名、客户、订单、组织关系或工作簿写入公开 Issue、PR、日志、截图或聊天。
3. 先预检，不写正式账本；由业务责任人确认差异。
4. 在独立库完成确认、逐月/整体对账、幂等重放、待补齐治理和恢复演练。
5. 各角色完成桌面 Web UAT 并签字；然后关闭 #63 和 #12。

### 6.2 公司服务器就绪后

1. 取得服务器、网络、域名、HTTPS、秘密、备份存储、监控、RPO/RTO、维护窗口和回退授权。
2. 按 `docs/deployment.md` 首装或升级；API 启动不得隐式迁移或初始化。
3. 验证可信代理、Origin、正式账号、最小权限、备份保留与新库恢复。
4. 接入监控告警并实测升级失败回退和核心 smoke。
5. 运维责任人签字；然后关闭 #64 和 #15。

### 6.3 P1 最终收口

当 #12、#15、#63、#64 全部关闭后：

1. 从最新 `origin/main` 重跑 `npm.cmd run verify`。
2. 重新做 Spec / Standards 双轴审查。
3. 更新 #65 验收矩阵并关闭 #65。
4. 更新 Roadmap #18。
5. 只有用户另行批准，才创建 Tag、GitHub Release 或真实部署。

## 7. 总体 Roadmap 与版本计划

| 阶段 | 状态 | 退出条件 |
| --- | --- | --- |
| P0 | 已完成 | 权限、组织、账本、目标、认证、导入与可重复验收进入 `main` |
| P1 工程波次 | 已完成 | 桌面 Web、分析、后台、审计、安全、可观测性、备份恢复和隔离 Compose 全绿 |
| P1 人工 Gate | 等待 | #63 真实数据 UAT、#64 公司服务器及父项/总验收关闭 |
| v1.1-alpha | 未正式发布 | 当前工程基线可内部评估；命名与发布仍需授权 |
| v1.1-rc | 未达到 | P1 全部 Gate、真实 UAT、干净部署、备份恢复和安全验收齐全 |
| v1.1 production | 未达到 | 公司服务器、HTTPS、正式凭据、生产数据、监控、回退及业务/运维签字齐全 |
| P2 | 未开始 | #16、#17、#35、#101 按单独范围与验收条件实施 |

继续保持模块化单体。没有真实吞吐、隔离或团队边界证据前，不引入微服务、消息队列、GraphQL、第二套数据库或第二套浏览器测试框架。

## 8. 绝对不要再踩的坑

1. **不要新建独立 worktree 或额外项目文件夹。** 用户明确反对 TB22 式额外目录；除非用户再次明确要求，只在现有 checkout 工作。
2. **不要丢弃用户改动。** 禁止 `reset --hard`、`clean`、广泛 `checkout/add/stash`；只用精确文件清单暂存。
3. **不要把真实数据叫示例数据。** 用户已明确示例文件属于真实敏感数据；按生产数据边界处理。
4. **不要泄漏业务正文。** 真实姓名、客户、订单、组织映射和工作簿不得进入公开 GitHub、日志或交接摘要。
5. **不要修改已应用迁移。** 新结构只追加迁移；同时验证空库、旧库升级和失败回滚。
6. **迁移哈希必须先统一换行。** 否则 Windows CRLF 与 Linux LF 会造成错误的篡改告警。
7. **所有数据库 bigint ID 都是字符串。** 不得在 API、前端、导出或导入中用 `Number()` 转换。
8. **不要把 `system_admin` 当业务超级账号。** 系统管理与业务读取/导出保持分离。
9. **不要恢复手写签名。** 使用已认证身份的显式确认和不可修改审计；普通系统日志不能单独替代目标版本确认。
10. **不要换回错误地图或占位 Logo。** 地图包含台湾省、不展示南海插图；Logo 使用 `apps/web/public/brand-logo.png`。
11. **受保护下载不能直接页面跳转。** 必须复用统一认证 fetch，正确处理 401/403 和错误正文。
12. **分页游标必须绑定快照和筛选。** 不得把累积结果伪装成可刷新复现的页面。
13. **不要批量清理 Docker。** 先核对项目名、容器、网络、卷、镜像和端口；只清理本次隔离验收资源。
14. **数据库作业脚本保持 LF，`start_all.bat` 保持 CRLF。** Windows 使用 `npm.cmd`。
15. **不要在测试运行时改源码。** 验收必须来自静止代码；修改后重新跑对应门禁。
16. **不要把 CI 绿色等同于真实 UAT、服务器或发布。** 四类证据分别取证、分别授权。
17. **不要自动恢复移动端、进入 P2、迁移真实数据或部署服务器。** 这些都需要新的明确条件或范围。
18. **GitHub 写入结果不确定时先查远端。** 不要因 TLS EOF 等瞬时错误重复建 Issue、PR、评论或关闭操作。

## 9. 接手顺序

1. 完整阅读本文件。
2. 阅读 `AGENTS.md`、`CONTEXT.md`、`docs/agents/`、相关 `docs/adr/` 与 `docs/specs/`。
3. 执行第 2.1 节的只读 Git/GitHub 复核。
4. 保护现有 checkout；不要新建 worktree。
5. 只有当真实数据或公司服务器条件明确就绪时，恢复对应人工 Gate。

接手原则：**实时证据优先；工程、真实数据 UAT、服务器验收和外部发布分别取证、分别授权。**
