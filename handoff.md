# SampleFlow 零上下文交接文档

> 更新时间：2026-08-27
>
> 当前阶段：**全项目严格审计已完成；v1.1 Gate A 尚未开始**
>
> 当前结论：这是可运行的技术原型，不是已经业务验收的 1.0，也不具备生产上线资格。

## 0. 新会话先做什么

不要从 README 的功能列表推断完成度，也不要直接继续写功能。按以下顺序恢复上下文：

1. 完整阅读本文件。
2. 阅读 [`CONTEXT.md`](CONTEXT.md) 和 [`docs/adr/`](docs/adr/)；它们保存稳定领域语言和已确认的难逆架构决策。
3. 实时查询 [Roadmap #18](https://github.com/Eclipseic1848/SampleFlow/issues/18) 及相关打开 Issue；GitHub 状态会变化，不要只信本文件的快照。
4. 对准备处理的 Issue，先冻结该 Issue 的范围和验收标准，再修改代码。
5. 执行只读状态核对：

   ```powershell
   git status --short --branch
   git rev-parse HEAD
   gh issue list --state open --limit 30
   docker ps
   Get-NetTCPConnection -State Listen -LocalPort 3000,5173,5174,55432 -ErrorAction SilentlyContinue
   ```

6. 第一个建议任务是 **Issue #1，仅建立隔离集成测试、端到端验收和 CI 基线**。不要自动跳到后续 Gate，也不要把修测试扩大成重写系统。

### 权威来源顺序

出现冲突时按此顺序处理，并向用户指出冲突：

1. 本文件：当前工程阶段、已验证状态、阻塞和接手动作。
2. GitHub Roadmap #18 与仍打开的 Issues：当前执行范围和任务追踪。
3. `CONTEXT.md` 与 ADR：稳定业务语言及已确认决策。
4. 本地原始 DOCX/Excel：原始需求和数据证据；这些文件禁止提交 Git。
5. 当前代码：它只证明“现在实现成什么样”，不能覆盖已确认业务规则。

## 1. 我们在做什么

SampleFlow 要把现有“轻流订单底表 + 人工复制到 Excel 核算 + 线下目标确认”的流程，建设为公司服务器部署的多用户 Web 系统。Windows 是开发环境，需要通过 `start_all.bat` 一键启动 PostgreSQL、后端热重载和前端热更新，以便业务负责人随时检查页面。

本轮原任务是：在此前 Issues 被认为“全部完成”后，从项目真实状态出发，完整阅读代码、配置、文档和原始材料，严格测试核心流程，重新制定下一步计划，并将任务放到 GitHub Issues 管理。审计发现大量流程并未闭环，因此当前工作不是发布 1.0，而是进入 **v1.1 产品闭环与上线准备**。

## 2. 产品领域与已经确认的业务规则

以下是已经稳定的核心口径。更完整的统一语言在 `CONTEXT.md`，难逆决定在 ADR 中；除非用户明确改变业务规则，否则不要重新发明定义。

### 2.1 数据来源和业绩账本

- 轻流是订单基础事实的权威来源；当前版本由销售助理逐条人工复制到 SampleFlow。
- 业绩入账后不可覆盖、不可删除，只能追加不可变事件。
- 营业额 110 调整为 100：追加 `-10`，不改原记录。
- 暂停与营业额修改是两个独立事件，只能先后发生；暂停按当时当前营业额全额扣减，重启按适用金额正向恢复。
- 只有当前计入金额为正的订单允许修改营业额。零金额订单可以录入但不计业绩，后续以“首次计入事件”转正。
- 调整计入操作发生月，不追溯篡改已签认月份。
- 同一订单编号承载首次入账、营业额修改、暂停、重启、首次计入等完整事件链。
- 2026 年 1—8 月迁移时只保留原 Excel 的业绩流水；协作数据不迁移，之后也不产生协作事件。

### 2.2 组织归属和汇总

- 每笔事件固化事件发生时的业务员、小组、部门、组长、主管快照。
- 人员或小组异动不改历史；异动生效后的旧订单调整事件归属新组织。
- 示例：张三 8 月在 A 组入账 100，9 月转到 B 组后暂停，则 A 组为 `+100`，B 组为 `-100`。
- 个人业绩与团队业绩分开；下属业绩不变成组长/主管个人业绩。小组、部门汇总只形成管理口径，不复制公司业绩。

### 2.3 角色与权限

| 角色 | 业绩编辑 | 目标编辑/下发 | 目标审批 | 查看/导出 |
|---|---:|---:|---:|---:|
| 销售助理、销售助理组长 | 是，仅有的业绩维护角色 | 否 | 否 | 按授权范围 |
| 业务员 | 否；可申请改本人目标 | 否 | 确认签名 | 按本人范围 |
| 业务员组长 | 否 | 下发业务员目标 | 确认本人小组目标 | 按小组范围 |
| 业务主管 | 否 | 下发组长目标 | 确认本人部门目标 | 按部门范围 |
| 销售经理 | 否，即使是高级管理者也无权改业绩 | 下发主管目标；录入自己的顶层总目标是唯一例外 | 签名本人总目标 | 销售管理范围 |
| 总经理 | 否 | 否 | 只审批销售经理总目标的第一道 | 查看、导出 |
| 人事部 | 否 | 否 | 所有目标最终审批 | 查看、导出 |
| 系统管理员 | 否，不自动拥有业务权限 | 否 | 否 | 不自动拥有业务查看/导出权限 |

销售助理和销售助理组长是监督者与数据录入执行者。业务员、组长、主管、经理是业绩达成责任人，不得修改业绩数据。人事和总经理只能查看、导出；导出副本可在系统外编辑，但不得回写系统原始记录。

### 2.4 目标流程

- 上级下达目标，责任人确认并签名；若责任人申请修改，由有权上级填写新目标，责任人重新签名，最后人事审批后生效。
- 每一级责任人都确认：销售经理、主管、组长、业务员。
- 销售经理总目标：销售经理依据公司金额录入并签名 → 总经理第一道审批 → 人事最终审批。总经理不参与其他层级。
- 其他层级：上级下达 → 本人签名 → 人事最终审批。
- 所有目标修改和目标确认都需人事最终审批；申请本身绝不能直接生效。
- 允许下级目标合计小于或大于上级目标。差额必须显式显示为“未分配”或“超分配”，并随最终版本由人事审批。
- 上级仍按自己的责任目标计算团队达成率，未分配差额不是上级个人销售目标；超分配也不自动提高上级目标。
- 下级目标变化后，上一级负责人选择是否调整自己的目标：选否，仅重算差额且原签名保持；选是，向更高一级申请，由有权上级修改，再重新签名和人事审批。该规则逐级递归。
- 未生效目标不能作为正式绩效依据，相关人员不能进入正式业绩报表。

## 3. 已经完成了什么

### 3.1 工程与代码骨架

- 单仓库 npm workspaces：`apps/api` 和 `apps/web`。
- 前端：React 19、Vite 8、TypeScript。
- 后端：Fastify 5、TypeScript。
- 数据库：PostgreSQL 16，已有 6 个 SQL migration。
- Windows：`start_all.bat` / `stop_all.bat`，前端 5174、API 3000、开发数据库 55432。
- 生产方向：Docker Compose + Nginx，但当前只属于未验收骨架。
- 已有登录、账号与组织维护、订单录入、不可变业绩事件、目标版本/签名/审批、看板和 CSV 导出的基础实现。
- 数据库层已对已入账业绩事件设置 UPDATE/DELETE 拒绝触发器。
- 已有 16 个 ADR，记录交付形态、PostgreSQL、账号、权限分离、不可变事件、组织快照、目标规则等决定。

### 3.2 原始数据和材料

本地工作目录有三份重要材料，均被 `.gitignore` 排除，不得提交到公开仓库或烘焙进镜像：

- `原始数据1.xlsx`：当前权威迁移源，2026 年 1—8 月；`分子` 表 4,701 条有效明细、2,850 笔订单、合计 14,675,659.07 元，59 名业务员，日期 2026-01-04 至 2026-08-26。
- `原始数据.xlsx`：旧的 1 月不完整文件，635 条且业务员为空。它不是迁移源，但在用户明确批准永久删除前作为来源证据保留。
- `销售到样业绩管理系统需求说明书.docx`：原始需求证据，包含手写签名、个人目标与时间进度预警、省份地图、客户单位排名/趋势/穿透和角色范围等需求。

历史导入当前已经得到 4,701 条 `legacy_adjustment` 事件和 2,850 笔订单，总额与 Excel 一致。导入“数字一致”不代表历史数据已经可正常用于业务流程，见下文阻塞。

### 3.3 GitHub 和任务管理

- 仓库：[Eclipseic1848/SampleFlow](https://github.com/Eclipseic1848/SampleFlow)
- 分支：`main`
- 本次交接开始前的本地/远端基线：`43a2e2c162693533b8eb4f144096e301848db41a`。接手时必须重新查询当前 SHA。
- 已创建 18 个打开 Issue：#1—#9 为 P0，#10—#15 为 P1，#16—#17 为 P2，#18 为总 Roadmap。
- 仓库当前是 **Public**、`main` 未配置分支保护、没有 CI。这是实时外部状态，接手时需重新确认。

### 3.4 本轮实际验证过的证据

以下是 2026-08-27 的实测，不是从代码表面推断：

- `npm.cmd run typecheck`：通过。
- `npm.cmd test`：4/4 通过，但只有业绩领域单元测试；前端实际为 0 个测试。
- `npm.cmd run build`：本地源码构建通过。
- `npm audit --omit=dev`：0 个已报告漏洞。
- `docker compose config`：通过。
- 数据库不可变触发器：直接 UPDATE/DELETE 已入账事件均被拒绝。
- 隔离临时数据库 API 严格测试：50 项中 31 通过、19 失败；临时数据库已删除，未污染开发库。
- 浏览器检查：主要页面可打开，桌面观感基本可用；移动端表格能横向滚动。
- Docker 镜像构建曾尝试，但拉取 Docker Hub Node/Nginx 层长期停滞后人工停止，**镜像构建未验证通过**。

这些通过项只是有限工程证据，不是业务 UAT、生产资格或上线授权。

## 4. 当前真实状态和卡点

### 4.1 开发数据状态

审计时开发数据库统计如下：

- 演示用户 9 个。
- 组织单元 0、组织任职 0。
- 订单 2,850、事件 4,701。
- 目标 0、目标版本 0、修改申请 0、联动选择 0。
- 所有迁移事件均为 `legacy_adjustment`，操作人、组长、主管快照为空。
- 订单投影：active 2,035、paused 87、zero 728；有 52 笔当前计入总额为负、763 笔为零。

组织为空的直接原因之一是迁移 `006_bootstrap_organization_from_ledger.sql` 在历史导入前执行。59 名 Excel 业务员尚未与系统账号和有效组织任职完整映射。因此“数据已导入”与“历史订单可按权限调整、按组织查看”之间仍有断链。

### 4.2 P0：阻塞业务闭环或任何上线讨论

| Issue | 当前缺口 | 为什么阻塞 |
|---|---|---|
| [#1](https://github.com/Eclipseic1848/SampleFlow/issues/1) | 没有可靠隔离集成测试、前端 E2E、CI 门禁 | 目前只能靠人工脚本发现回归，所谓“全部 Issues 完成”无法被证明 |
| [#2](https://github.com/Eclipseic1848/SampleFlow/issues/2) | 服务端业务数据范围未闭合 | 实测业务员可读取其他人的订单；系统管理员可读取业务看板 |
| [#3](https://github.com/Eclipseic1848/SampleFlow/issues/3) | 组织初始化、有效期、重叠任职、跨部门约束和服务端事件快照缺失 | 当前允许跨部门/重叠/不存在上级的任职；组织快照可被客户端输入，历史数据无组织链 |
| [#4](https://github.com/Eclipseic1848/SampleFlow/issues/4) | 历史订单不可正常调整，页面无完整事件链 | 导入事件缺少操作者与组织；调整表单强制要求不存在的组长/主管，必须编造数据才可提交 |
| [#5](https://github.com/Eclipseic1848/SampleFlow/issues/5) | 可回填历史业务时间，没有记账月关闭/重开治理 | 实测 2026 订单可新增 2025 事件并排到首次入账之前，破坏不可篡改月份原则 |
| [#6](https://github.com/Eclipseic1848/SampleFlow/issues/6) | 目标层级、所有者资格、职责分离未由后端闭合 | 可建立无父级部门目标；HR 可成为目标责任人、签名并自行审批；非法父级返回 500 并暴露约束 |
| [#7](https://github.com/Eclipseic1848/SampleFlow/issues/7) | 修改申请只有创建，没有列表、处理、重签、联动与完整审计 | 表已建但流程半成品，不能称为闭环 |
| [#8](https://github.com/Eclipseic1848/SampleFlow/issues/8) | 首次改密只在前端提示，服务端未强制；密码基线不足 | 实测 8 位临时密码可用，未改密账号可直接调用业绩写 API |
| [#9](https://github.com/Eclipseic1848/SampleFlow/issues/9) | 历史导入与启动耦合，全新克隆无法构建部署 | Dockerfile 依赖不进 Git 的 Excel，并把迁移、seed、导入和服务启动绑在一起 |

19 个隔离 API 失败覆盖的代表性事实：

- `system_admin` 读取看板返回 200。
- 业务员能看到他人订单。
- 没有生效目标的用户能进入正式业绩看板。
- 回填 2025 事件被接受。
- 无父级部门目标可签名并由 HR 激活。
- HR 可被设为部门目标负责人并自行签名、自行审批。
- 不存在父目标导致 500，而非受控 4xx。
- 修改申请创建后无任何查询或处理 API。
- 同名小组被全局唯一限制，不能在不同部门复用。
- 跨部门任职、不存在组长/主管、重叠任职均被接受。
- 8 位临时密码被接受。
- 未完成首次改密仍能直接调用业绩写 API。

已通过且应防止回归的核心行为：

- 未认证请求 401。
- `system_admin`、`sales_manager` 的业绩写请求 403。
- 销售助理可录订单，重复编号 409。
- 110 → 100 追加 `-10`，暂停追加 `-100`，暂停时改金额 409，重启追加 `+100`。
- 零金额首次计入路径可工作。
- 销售经理顶层目标签名后，HR 不能跳过总经理；总经理通过后 HR 才可最终激活。
- 导出角色限制的基础检查通过。

### 4.3 P1：P0 稳定后必须补齐

- [#10](https://github.com/Eclipseic1848/SampleFlow/issues/10)：按角色目标达成看板、时间进度预警和无生效目标时的报表门禁。
- [#11](https://github.com/Eclipseic1848/SampleFlow/issues/11)：订单搜索、筛选、分页、条件导出。当前固定取 100 条，无可用搜索分页。
- [#12](https://github.com/Eclipseic1848/SampleFlow/issues/12)：省份地图、客户单位排名/趋势/明细穿透。
- [#13](https://github.com/Eclipseic1848/SampleFlow/issues/13)：账号、组织、审批和审计后台产品体验。
- [#14](https://github.com/Eclipseic1848/SampleFlow/issues/14)：模块拆分、可访问性、移动端。浏览器审计发现 6 个导航按钮无可访问名称、108 个颜色对比节点问题。
- [#15](https://github.com/Eclipseic1848/SampleFlow/issues/15)：生产安全、备份恢复、可观测性和部署验收。

原 DOCX 明确出现的手写签名、目标/时间双进度预警、省份和客户分析尚未完整实现，也没有 ADR 明确将其排除。不能因现有页面有一个字符串“签名”就判定需求完成。

### 4.4 代码和基础设施债务

- `apps/web/src/App.tsx` 约 41 KB，页面、状态、请求和交互集中在一个文件。
- 后端 route 模块同时承担校验、授权、SQL 和事务，边界浅；只有 `domain/performance.ts` 相对可测试。
- 建议保持模块化单体，不要现在拆微服务。逐步形成 `AuthorizationPolicy`、`OrgResolver`、`PerformanceLedger`、`GoalWorkflow`、`ImportJob`、`ReportingProjection` 六个深模块。
- 没有 OpenAPI 或前后端共享契约、lint、覆盖率门槛、CI、结构化生产日志、监控告警、备份恢复演练和分支保护。
- 目标页面使用原始用户名/数据库 ID；签名和修改申请使用 `window.prompt`；表单通用 `required` 属性误伤可选字段。

## 5. 当前运行和本地开发

2026-08-27 交接时核对到：

- SampleFlow API 监听 3000。
- SampleFlow Vite 前端监听 5174。
- `sampleflow-db-1` 健康，PostgreSQL 映射到 55432。
- 5173 是另一个 Mangrove 项目，**绝对不要停止或占用**。

开发账号名等于角色代码，统一初始密码 `SampleFlow@2026`。包括 `system_admin`。这些是公开仓库可见的演示凭据，只能用于本地开发；首次改密尚未由服务端闭合。

若需要启动：先启动 Docker Desktop，再双击 `start_all.bat`。停止本项目数据库用 `stop_all.bat`，但操作前先核对目标容器，禁止批量停止其他项目容器。

## 6. 下一步执行计划

### Gate A：先建立可信验证基线

1. 执行 Issue #1。
2. 把本轮 19 个失败场景转成可重复运行的隔离数据库测试，先保持红色并标注对应 Issue。
3. 增加关键浏览器 E2E：登录/首次改密、订单事件链、目标签名审批、角色数据范围。
4. 在 CI 中运行类型检查、单元测试、隔离集成测试、前端构建和基础 E2E。
5. 证明测试不会污染开发数据库，并记录运行时间和失败证据。

成功标准不是“测试脚本存在”，而是干净环境可重复运行、失败能阻断 CI、现有 19 个缺口被稳定复现。

### Gate B：冻结权限、记账和认证边界

按 #2 → #5 → #8 处理：

- 先由业务负责人确认逐角色读取范围矩阵，特别是销售经理与两类销售助理的完整可见范围。
- 所有列表、详情、看板、导出和写接口统一使用服务端授权策略。
- 事件操作时间由服务端确定；建立记账月关闭及受控重开规则。
- 首次改密、禁用账户、临时密码和会话失效必须由服务端强制。

### Gate C：恢复可信数据底座与可部署性

按 #3 → #9 处理：

- 冻结有效期组织模型与导入映射方案。
- 由服务器端根据事件发生时间解析组织快照，客户端不能填写权威快照。
- 把迁移、生产初始化、历史导入和应用启动拆成独立作业。
- 原始 Excel 通过受控挂载/上传进入导入作业，不进入 Git，不进入最终镜像。
- 在全新克隆和隔离数据库验证部署、重放、幂等、失败恢复。

### Gate D：闭合两条核心业务链

按 #4 → #6 → #7 处理：

- 历史与新订单都能查看完整事件链，并按业务规则调整。
- 目标所有者、父子层级、编辑人、签名人和审批人资格全部由服务端验证。
- 补齐修改申请 → 有权上级填写新目标 → 责任人重签 → 人事审批 → 生效。
- 补齐下级变化后的“是否调整本级目标”选择、差额重算与逐级申请。
- 以角色化 E2E 证明正向、拒绝和边界路径。

### P1 打磨与上线候选

P0 全部验收后再按 #10—#15 推进：报表、搜索导出、分析、后台 UX、模块化、可访问性、部署与运维。不要在 P0 权限和数据模型未冻结前大规模重做 UI。

### P2 后续能力

- #16：轻流只读自动同步。当前仍是人工逐条复制，不要未经决策接入外部 API。
- #17：评估 2024—2025 历史明细迁移。当前明确暂不迁移明细。

## 7. 总体 Roadmap 和版本计划

### v1.1-alpha：内部试用资格

范围：完成 #1—#9 全部 P0 Gate，并通过隔离集成和端到端验收。

定义：可供受控内部试用，不是生产发布。

粗略工作量：41—68 人日；单人约 8—14 周，取决于业务决策响应和测试返工。

### v1.1-rc：生产候选资格

范围：在 alpha 基础上完成选定 P1，至少包括正式报表门禁、可用的查询导出、后台关键流程、部署安全、备份恢复和可观测性。

定义：可进入公司服务器验收，不等于已经上线。

P1 粗略工作量：29—47 人日。

### v1.1 正式生产

范围：公司服务器、域名/HTTPS、内网访问、正式账号、密钥、数据导入、备份恢复、监控告警、UAT 和上线回退均由责任人验收。

定义：只有业务负责人和服务器/安全责任人明确签字后，才可称为正式上线。代码合并或测试绿色不能自动获得发布授权。

### v1.2+ 候选方向

- 轻流只读自动同步。
- 2024—2025 历史数据迁移。
- 更丰富的省份、客户、趋势和管理分析。
- 在真实数据规模和使用频率证明有必要后，再评估缓存、异步导出或读模型优化；现在不要提前拆微服务。

## 8. 仍需用户/责任人决定的问题

这些不能由新会话静默替用户决定：

1. **GitHub 仓库是否改为 Private。** 当前仓库是 Public；虽然原始 Excel/DOCX 未提交，但内部业务规则和演示凭据公开。改变可见性属于外部治理动作，需要仓库所有者明确批准。
2. **角色读取范围矩阵。** 特别是销售经理、销售助理、销售助理组长是否看全公司、指定部门或其他范围。
3. **记账月关闭治理。** 谁能关账、何时自动/人工关闭、谁能申请重开、谁审批、重开后如何重新签认。
4. **原 DOCX 功能范围。** 手写签名、省份地图、客户单位排名/趋势/穿透是否必须进入 v1.1；若排除，必须新增 ADR 记录。
5. **生产环境。** 域名、HTTPS、仅内网或其他访问策略、备份保留期、恢复目标、正式管理员与密钥托管方式。
6. **旧 `原始数据.xlsx` 的处置。** 它不是当前迁移源，但在用户明确授权永久删除前保留；不要自行删除来源证据。

### 新会话第一次需要的授权

建议先向用户展示本文件与 #1 的验收范围，然后请求明确的执行授权：

> “是否开始执行 Issue #1，仅建立隔离测试/E2E/CI 基线，不修复 #2—#9 的业务缺口？”

同时可以单独提示 Public 仓库风险，但不要把“开始 #1”解释为允许修改仓库可见性、生产部署或删除原始材料。

## 9. 绝对不要再踩的坑

1. **Issue 被关闭、单元测试绿色、页面能点，不等于业务闭环。** 必须按角色、数据范围、异常路径和真实状态变化验证。
2. **不要只在前端做权限、首次改密或报表门禁。** 直接 API 调用必须同样被服务端拒绝。
3. **不要让客户端填写组织快照。** 服务端必须按事件发生时间和有效任职解析并固化。
4. **不要允许客户端把事件回填到历史已签认月份。** 操作时间以服务端为准；关账和重开必须有治理与审计。
5. **不要把迁移、生产 seed、历史导入和服务器启动绑在一起。** 它们必须是可单独执行、审计、重放和失败恢复的作业。
6. **不要把原始 Excel、客户/员工数据提交 Git 或烘焙进镜像。** `.gitignore` 不是数据治理方案，构建也不能依赖本地秘密文件。
7. **不要让业务用户手填用户名或数据库 ID。** 使用受权限和组织约束的可搜索选择器。
8. **不要只建表就宣称流程完成。** 修改申请、联动选择已有数据结构但缺少完整 API/UI，这是典型半成品。
9. **不要用一个通用 `required` 组件覆盖所有字段。** 当前它使“可选父目标”和历史订单调整表单实际无法使用。
10. **不要把本地源码 build 或 `docker compose config` 说成 Docker/生产部署通过。** 镜像、干净安装、升级、备份恢复和公司环境都必须分别验证。
11. **不要因为旧材料不再是权威源就永久删除。** 标记为非权威并保留证据，除非用户明确批准删除。
12. **不要触碰 5173 或批量停止 Docker 容器。** 5173 和多个容器属于其他项目；SampleFlow 使用 5174/3000/55432。
13. **不要使用 `git reset --hard`、`git clean` 或广泛 checkout。** 保护用户本地 Excel、DOCX 和其他未跟踪工作。
14. **不要先大改 UI 或拆微服务。** 先冻结权限、组织、账本和目标状态机，再做产品体验和模块深化。
15. **不要把 demo 密码用于生产。** 当前 `SampleFlow@2026` 和角色账号只用于本地开发，且首次改密仍有服务端漏洞。

## 10. 交接时的证据边界

### 已确认事实

- 本地开发服务可运行；基础类型检查、4 个领域单测、本地构建通过。
- 50 项隔离 API 审计中 19 项失败，失败类型已映射到 P0 Issues。
- 2026 年历史数据数字已导入并与 Excel 总额一致。
- 当前组织、账号映射、目标实际数据为空或未闭合。
- 当前 Dockerfile 对不进 Git 的 Excel 有硬依赖；镜像构建未成功验证。

### 基于代码和现状的架构判断

- 继续采用模块化单体最合适；现阶段拆微服务只会放大事务、权限和测试成本。
- `AuthorizationPolicy`、`OrgResolver`、`PerformanceLedger`、`GoalWorkflow`、`ImportJob`、`ReportingProjection` 是优先深化的边界。

### 尚未验证/尚未授权

- 未执行真实公司服务器部署、正式数据迁移、HTTPS、并发/负载、备份恢复、故障切换或业务 UAT。
- 未获得把仓库改为 Private、正式发布、删除旧 Excel、接入轻流或迁移 2024—2025 数据的授权。
- 未开始 #1—#17 的修复实现；本轮只完成审计、任务化、文档去误导和交接。

---

接手原则：**先用失败测试固定事实，再逐 Gate 闭合；任何“完成”都必须带可复现验收证据。**
