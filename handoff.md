# SampleFlow 零上下文交接

> 更新时间：2026-08-30（America/Los_Angeles）
>
> 当前阶段：**原 P0 #1—#9 已闭环；全仓复审后的加固 #30—#34 已在本地分支实现并通过门禁；尚未进入 P1、生产数据落库或生产发布**
>
> 当前结论：加固代码位于 `codex/audit-hardening`，尚未推送、创建 PR 或关闭 Issue；这仍是内部验收基线，不是生产上线证明。

## 0A. 2026-08-30 全仓复审后加固暂停点

- 用户授权实现非 P1 的复审修复，并授权创建相关 GitHub Issue；P1 是否启动仍由用户另行决定。
- 已创建并保持打开：#30 登录限流并发、#31 Excel 解压资源、#32 未知异常脱敏、#33 导入查询性能、#34 生产前端默认凭据；#35 记录为 P2 CI 加固，不在本轮实现。
- 本地 `codex/audit-hardening` 已实现 #30—#34；全量 `npm.cmd run verify` 通过：API 122/122、Playwright 10/10、类型检查、构建、生产依赖审计和 Compose 配置均通过。
- 4,701 行等价合成数据的导入预检读取查询保持不超过 12 次，确认读取查询不超过 10 次；测试只使用合成数据，没有输出真实业务明细。
- 本地 diff 已完成主会话验收和独立只读审查；审查提出的异常状态码伪装、组织工作簿二次读取和生产 Bundle 持久门禁缺口均已修复并复验。
- 没有授权推送分支、创建/合并 PR、评论或关闭 Issue。若用户批准，再单独发布加固 PR；不要自动开始 #10—#15。

## 0. 新会话先做什么

1. 完整阅读本文件。
2. 阅读 [`AGENTS.md`](AGENTS.md)、[`CONTEXT.md`](CONTEXT.md)、[`docs/agents/`](docs/agents/) 和相关 [`docs/adr/`](docs/adr/)；当前仓库没有 `CLAUDE.md`。
3. 实时核对 GitHub，不要只信本文件的快照：

   ```powershell
   git status --short --branch
   git fetch origin main
   git rev-parse HEAD
   git rev-parse origin/main
   gh issue list --repo Eclipseic1848/SampleFlow --state open --limit 30
   gh pr list --repo Eclipseic1848/SampleFlow --state open
   gh api repos/Eclipseic1848/SampleFlow/branches/main/protection
   ```

4. 核对本机状态后再启动服务，不能默认端口空闲：

   ```powershell
   docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
   Get-NetTCPConnection -State Listen -LocalPort 3000,5173,5174,55432 -ErrorAction SilentlyContinue
   ```

5. 原 P0 #1—#9 不再继续；复审新增 #30—#34 仍为打开状态，本地实现尚未发布。先核对本文件 0A、GitHub 和 `codex/audit-hardening`。
6. 只有在用户明确批准并完成加固发布后，才让用户选择进入 P1、准备业务 UAT或制定真实数据/生产迁移方案。若用户选择 P1，先读取 Roadmap #18 和 Issue #10，冻结范围与验收标准；不要自动实现 #11—#15。

### 权威来源顺序

发生冲突时按以下顺序处理并向用户报告：

1. 用户当前明确决定，尤其是业务范围、数据含义、权限、发布和不可逆操作。
2. GitHub 当前 Issue、PR、分支保护和 CI 状态。
3. 本文件的当前工程事实、边界和暂停点。
4. `CONTEXT.md` 与 ADR 中已经确认的领域规则。
5. 本地真实 Excel/DOCX 来源证据；它们不得进入 Git、镜像或日志。
6. 当前代码；代码只证明“实现成什么样”，不能自行覆盖业务定义。

## 1. 本轮目标与完成状态

本轮目标是完成仓库所有 `priority:P0` GitHub Issues。结果：

- Issue #1—#9 已全部关闭。
- 原 P0 收口时 `priority:P0` 为 Open (0)；本次复审后新增 #30—#34，当前为打开状态，详见 0A。
- 最后一个实现 PR 是 [#27](https://github.com/Eclipseic1848/SampleFlow/pull/27)，已合并。
- P0 代码基线 merge SHA：`5f7523ac34699bb0c32a4015da4a5f46f8874655`。本交接文档的合并会继续推进 `main`，新会话必须实时查询当前 SHA。
- PR #27 必需 CI [run 33353498614](https://github.com/Eclipseic1848/SampleFlow/actions/runs/33353498614) 完成且结论为 `success`。

本轮授权仅覆盖本地实现 #30—#34 和创建 #30—#35，不包含推送、PR、关闭 Issue、P1、生产导入、发布、删除文件或修改仓库治理。

## 2. P0 交付结果

### 2.1 可信工程门禁

- `main` 受保护：必须走 PR；required check 为 `Typecheck, test, build and audit`；strict 为 true；管理员也受约束；禁止 force-push 和删除分支。
- CI 使用 Node.js 24、PostgreSQL 16、`npm ci`、Chromium，并执行根目录 `npm run verify`。
- 当前门禁包含 API 单元/隔离集成测试、前端 Playwright E2E、前后端类型检查与构建、生产依赖审计、Compose 配置解析。
- PR #27 本地最终证据：API 117/117，Web Playwright 10/10，类型检查和构建通过，Compose 配置通过。
- 本地最后一次 `npm audit` 重试遇到机器 TLS 断连；同一锁文件此前审计为 0 漏洞，随后 PR 必需 CI 完整通过，因此远端门禁是最终证据。

### 2.2 权限、认证与账号

- 服务端统一限制读取、导出和写入范围，不能仅靠前端隐藏按钮。
- `system_admin` 本身不自动拥有业务读取、导出或业绩编辑权。
- 账号管理页提供与服务端授权同源的只读角色权限矩阵，便于管理员理解角色组合。
- 密码规则由服务端统一执行：6—128 位，必须同时含英文字母、数字和符号；界面提示强度。
- 登录和首次改密页均有密码显示/隐藏控件。
- 初始/重置密码随机生成、只显示一次、强制首次改密；首次改密前业务 API 被服务端拒绝。
- 会话、Origin/CSRF、登录限速、临时密码过期、停用/重置撤销会话已有自动化覆盖。
- 当前开发库存在验收账号 `super_admin`，状态 active、已完成首次改密，组合全部 9 个既有角色；它不是新增的绕过型“超级角色”。密码不写入本文件、Git、日志或审计。

### 2.3 组织、账本与目标流程

- 人员身份与登录账号分离；事件使用稳定人员标识。
- 组织任职、父子关系、同名小组、跨部门错配、重叠有效期和负责人连续性由服务端/数据库约束。
- 每条业绩事件按发生日期解析并固化人员、部门、小组、组长和主管快照；后续调组不改历史。
- 历史订单允许业务员变化：每条事件归属该行当时人员/组织，订单当前业务员取最后一条历史事件。
- 不可变账本、关闭记账期间、一次性更正授权、历史待核订单治理已有隔离测试。
- 目标的父子层级、所有者资格、签名、总经理/人事审批、修改申请、递归联动选择和正式报表门禁已有服务端与浏览器测试。

### 2.4 Excel 受控导入

- 除人工录入外，系统支持未加密 `.xlsx` 的显式上传、预检和确认。
- 原始文件不随 API 启动导入，不进入 Git、镜像或日志；数据库迁移、管理员初始化、开发 seed、历史导入是独立作业。
- 普通导入保持订单基础事实严格一致。
- 仅专用 `legacy_adjustment` 历史配置允许同一订单的业务员、客户单位、来源区域和服务类型随历史行变化。
- 每行原值保存在批次证据；每条事件保存该行业务员和当时组织；订单当前投影取**全量历史**中按发生日期、来源行号排序后的最后一行。
- 客户姓名仍严格一致；已经存在普通业务事件的订单不能套用历史例外。
- 稳定来源键、跨文件重复候选、整体/逐月对账、原子确认、失败回滚、核对旧事件和重复执行幂等已有测试。

## 3. 真实 Excel 的已验证事实

用户已明确：`原始数据1.xlsx` 不是虚构示例，而是真实数据。按敏感真实业务数据处理。

### 3.1 来源与基线

- 权威文件：`原始数据1.xlsx`；Git 未跟踪。
- SHA-256：`926aad3d8c59cc356094eb1abc0ca1fcb3392eae5867f2b7c0e2bb50bb5c01cf`。
- 总计：4,701 行/事件、2,850 笔订单、14,675,659.07。
- 正确逐月基线：

| 月份 | 事件数 | 金额 |
|---|---:|---:|
| 2026-01 | 635 | 2,314,819.55 |
| 2026-02 | 455 | 1,252,546.10 |
| 2026-03 | 560 | 1,346,159.95 |
| 2026-04 | 520 | 1,989,517.64 |
| 2026-05 | 474 | 1,499,121.10 |
| 2026-06 | 631 | 2,234,990.59 |
| 2026-07 | 778 | 2,487,624.14 |
| 2026-08 | 648 | 1,550,880.00 |

### 3.2 隔离功能验收

真实文件只在隔离临时数据库完成了功能验收，没有写入生产或修正当前开发库：

- 预检状态 `preflight_ready`。
- 阻断 0；`HISTORICAL_REVIEW_REQUIRED` 警告 199 条。
- 确认结果：4,701 事件、2,850 订单、14,675,659.07；整体与逐月完全匹配。
- 相同文件重放不新增事件；再次确认总数和金额不变。
- 验收输出没有打印员工或客户明细。

### 3.3 当前开发库不是正确迁移结果

2026-08-30 的只读核对：

- 容器 `sampleflow-db-1` healthy，仅数据库监听 55432；API 3000、前端 5174 当时未运行。
- 当前开发库：11 users、11 people、0 org_units、0 org_memberships、2,850 orders、4,701 events、0 import_batches、0 goals、18 migrations。
- 历史金额仍为 14,675,659.07，但日期范围是 2026-01-03 至 2026-08-25，比正确 Excel 日期整体早一天；50 行因此跨月。
- 原因是旧迁移路径把 Excel 日期经本地 `Date`/PostgreSQL 时区转换后落库。当前导入解析和隔离验收已使用正确日期，但旧开发库没有自动修复。

禁止在没有数据库备份、目标库确认、迁移方案、回滚方案和用户明确授权时修正当前开发库。优先考虑从正确迁移作业重建验收库，不要手写批量 `+1 day` 覆盖真实账本。

## 4. 尚未完成且仍由人工控制

以下不属于“P0 工程完成”，也没有被静默授权：

1. **真实人事映射与组织落库**：59 名历史业务员的人员身份状态可预检，但真实部门、小组、组长、主管及生效日期必须由人事确认；当前开发库仍没有组织数据。
2. **真实环境数据迁移**：隔离验收不等于开发、验收或生产数据库已经正确导入。
3. **业务 UAT**：尚未由各角色业务人员完成正式验收和签字。
4. **生产部署**：未验证公司服务器、域名/HTTPS、正式密钥、内网策略、备份恢复、监控告警、故障回退和负载。
5. **Docker 生产镜像**：源码构建、测试和 Compose 配置通过，不等于最终生产镜像和公司环境部署通过。
6. **仓库可见性**：仓库仍可未登录公开读取。是否改 Private 属于仓库治理决定，需要所有者明确批准。
7. **来源文件处置**：`原始数据.xlsx` 虽不是当前权威源，仍是来源证据；未经明确批准不得永久删除。
8. **外部集成与发布**：没有授权接入轻流 API、发布版本/标签、部署或对外公告。

## 5. GitHub 当前任务面

### 已关闭

- #1—#9：全部 P0，已完成并关闭。

### 仍打开

- [#10](https://github.com/Eclipseic1848/SampleFlow/issues/10)：按角色目标达成看板、时间进度预警与报表门禁。
- [#11](https://github.com/Eclipseic1848/SampleFlow/issues/11)：订单搜索、筛选、分页与条件导出。
- [#12](https://github.com/Eclipseic1848/SampleFlow/issues/12)：省份、客户单位多维分析和穿透。
- [#13](https://github.com/Eclipseic1848/SampleFlow/issues/13)：账号、组织、审批和审计后台体验。
- [#14](https://github.com/Eclipseic1848/SampleFlow/issues/14)：模块拆分、可访问性和移动端。
- [#15](https://github.com/Eclipseic1848/SampleFlow/issues/15)：生产安全、备份恢复、可观测性与部署验收。
- [#16](https://github.com/Eclipseic1848/SampleFlow/issues/16)：轻流只读自动同步评估。
- [#17](https://github.com/Eclipseic1848/SampleFlow/issues/17)：2024—2025 历史明细迁移评估。
- [#18](https://github.com/Eclipseic1848/SampleFlow/issues/18)：v1.1 Roadmap。
- [#30](https://github.com/Eclipseic1848/SampleFlow/issues/30)：登录限流并发计数原子性。
- [#31](https://github.com/Eclipseic1848/SampleFlow/issues/31)：Excel 解压资源边界。
- [#32](https://github.com/Eclipseic1848/SampleFlow/issues/32)：未知 API 异常响应脱敏。
- [#33](https://github.com/Eclipseic1848/SampleFlow/issues/33)：Excel 导入查询与扫描性能。
- [#34](https://github.com/Eclipseic1848/SampleFlow/issues/34)：生产前端移除默认开发凭据。
- [#35](https://github.com/Eclipseic1848/SampleFlow/issues/35)：P2 CI 供应链、覆盖率和浏览器矩阵加固。

## 6. 建议下一步与版本计划

### 下一步：等待用户选择

建议让用户在以下两条路径中明确选择，不能代替用户决定：

1. **进入 P1 产品闭环**：先处理 #10；先核对原 DOCX 与现有目标/报表实现，冻结角色视图、时间进度口径和验收页面，再实现。
2. **准备真实 UAT/迁移**：先产出不写库的迁移方案，明确目标环境、备份/恢复、人事映射、日期纠正、数据校验和批准人；未经确认不执行。

### 版本 Gate

- **v1.1-alpha（当前工程位置）**：P0 功能和自动化门禁完成，可作为隔离内部功能验收基线；尚未取得真实组织数据、业务 UAT 和部署资格。
- **v1.1-rc**：按批准范围完成 P1 #10—#15，至少包含正式报表、可用查询导出、后台关键体验、可访问性和生产运维验收。
- **v1.1 production**：公司服务器、HTTPS/网络、正式账号与密钥、真实数据迁移、备份恢复演练、监控、UAT 和回退全部由责任人签字。
- **v1.2+**：仅在独立决策后考虑 #16 轻流只读同步、#17 旧年度数据和更高级分析；不要提前拆微服务或引入异步基础设施。

## 7. 容易踩的坑

1. 真实 Excel 是敏感真实数据；不要把行级姓名、客户、密码、文件内容或映射输出到日志、PR、Issue 或聊天摘要。
2. 当前开发库日期早一天且组织为空；页面“有 4,701 条数据”不代表数据可用于验收。
3. 历史订单当前投影取全量历史最后一行，不是本批次最后一行；补录旧行不能让投影倒退。
4. 历史例外不能覆盖客户姓名，也不能污染已经有普通业务事件的订单。
5. 事件组织快照不可用当前组织重算；调组后的正负事件属于各自发生时组织。
6. `system_admin` 不等于业务全权限；`super_admin` 是为验收组合 9 个现有角色，不应成为绕过服务端授权的特殊代码路径。
7. 首次改密错误要围绕当前登录账号、临时密码和会话排查，不要拿其他演示账号替代用户复现。
8. 5174/3000/55432 是 SampleFlow 默认端口；用户有多个程序同时运行，启动和停止前必须核对端口、PID、容器名，禁止批量结束其他项目。
9. 测试绿色、CI success、源码 build、Compose config 都不等于生产镜像、部署、UAT 或发布。
10. 不要把 P0 关闭当成 P1 授权；外部写入、发布、真实数据、仓库可见性和不可逆操作仍需单独确认。
11. 不要使用 `git reset --hard`、`git clean`、广泛 checkout/add；只处理明确文件并保护用户材料。
12. GitHub/API 偶发 TLS EOF；不要因一次网络失败重复创建 PR、批次或其他外部对象，先读取远端状态再重试。

## 8. 证据分类

### 已验证事实

- 原 P0 #1—#9 已关闭；复审新增 P0 #30—#34 当前保持打开，本地实现尚未发布。
- `origin/main` 的 P0 代码基线为 `5f7523ac...`；PR #27 必需 CI success。
- 本地 API 117/117、Web E2E 10/10、类型检查、构建和 Compose 配置通过。
- 真实工作簿在隔离库中精确导入、逐月匹配且重放幂等。
- 当前开发库日期早一天、组织为空、没有受控导入批次；没有被本轮修改。
- `main` 分支保护已启用并强制必需 CI。

### 基于代码和现状的推断

- 当前最合适的架构仍是模块化单体；P1 应沿现有授权、组织、账本、目标和导入模块深化，不应拆微服务。
- 当前开发库更适合在备份后通过正确作业重建，而不是直接修补不可变事件；最终方案仍需数据负责人批准。

### 尚未验证或尚未授权

- 真实人事负责人映射、正式组织导入、开发库日期纠正。
- 公司服务器部署、生产镜像、HTTPS、正式密钥、备份恢复、监控、负载和故障演练。
- 业务 UAT、版本发布、标签、生产迁移、仓库改 Private、删除来源文件、轻流接入。

---

接手原则：**先实时核对，再冻结当前阶段；实现、数据迁移、UAT、发布和生产部署分别取证、分别授权。**
